// ============================================================================
// PERFIL DE JUGADOR POR DEFECTO
// ============================================================================
// Define la estructura inicial de un perfil de jugador ranked
// ============================================================================

import { getRank } from './game';

// ============================================================================
// CREADOR DE PERFIL POR DEFECTO
// ============================================================================

export const createDefaultPlayerProfile = (overrides = {}) => ({
  // ─────────────────────────────────────────────────────────────────────────
  // Identificación
  // ─────────────────────────────────────────────────────────────────────────
  id: overrides.id || `player_${Date.now()}_${Math.random().toString(36).substr(2, 9)}`,
  name: overrides.name || 'Jugador',
  avatar: overrides.avatar || '😎',
  
  // ─────────────────────────────────────────────────────────────────────────
  // Rating Glicko-2
  // ─────────────────────────────────────────────────────────────────────────
  rating: overrides.rating ?? 1500,
  rd: overrides.rd ?? 350,           // Rating Deviation (incertidumbre)
  volatility: overrides.volatility ?? 0.06,
  peakRating: overrides.peakRating ?? 1500,
  
  // ─────────────────────────────────────────────────────────────────────────
  // Placement
  // ─────────────────────────────────────────────────────────────────────────
  isInPlacement: overrides.isInPlacement ?? true,
  placementGames: overrides.placementGames ?? 0,
  placementWins: overrides.placementWins ?? 0,
  
  // ─────────────────────────────────────────────────────────────────────────
  // Estadísticas de temporada
  // ─────────────────────────────────────────────────────────────────────────
  seasonId: overrides.seasonId ?? 1,
  seasonWins: overrides.seasonWins ?? 0,
  seasonLosses: overrides.seasonLosses ?? 0,
  seasonDominoes: overrides.seasonDominoes ?? 0,
  seasonTrancas: overrides.seasonTrancas ?? 0,
  
  // ─────────────────────────────────────────────────────────────────────────
  // Estadísticas totales
  // ─────────────────────────────────────────────────────────────────────────
  totalGames: overrides.totalGames ?? 0,
  wins: overrides.wins ?? 0,
  losses: overrides.losses ?? 0,
  stompWins: overrides.stompWins ?? 0,  // Victorias aplastantes (para detección smurf)
  
  // ─────────────────────────────────────────────────────────────────────────
  // Rachas
  // ─────────────────────────────────────────────────────────────────────────
  currentStreak: overrides.currentStreak ?? 0,
  maxStreak: overrides.maxStreak ?? 0,
  
  // ─────────────────────────────────────────────────────────────────────────
  // Monedas
  // ─────────────────────────────────────────────────────────────────────────
  tokens: overrides.tokens ?? 0,
  coins: overrides.coins ?? 0,
  
  // ─────────────────────────────────────────────────────────────────────────
  // Fechas
  // ─────────────────────────────────────────────────────────────────────────
  createdAt: overrides.createdAt ?? Date.now(),
  lastGameDate: overrides.lastGameDate ?? Date.now(),
  lastLoginDate: overrides.lastLoginDate ?? Date.now(),
  
  // ─────────────────────────────────────────────────────────────────────────
  // Daily Rewards
  // ─────────────────────────────────────────────────────────────────────────
  dailyStreak: overrides.dailyStreak ?? 0,
  lastDailyClaimDate: overrides.lastDailyClaimDate ?? null,
  
  // ─────────────────────────────────────────────────────────────────────────
  // Flags de protección anti-abuse
  // ─────────────────────────────────────────────────────────────────────────
  smurfScore: overrides.smurfScore ?? 0,
  boostingWarnings: overrides.boostingWarnings ?? 0,
  
  // ─────────────────────────────────────────────────────────────────────────
  // Cosméticos equipados
  // ─────────────────────────────────────────────────────────────────────────
  equipped: overrides.equipped ?? {
    tile: 'classic_white',
    board: 'felt_green',
    avatar: 'default_1',
    frame: null,
    title: null
  },
  
  // ─────────────────────────────────────────────────────────────────────────
  // Inventario
  // ─────────────────────────────────────────────────────────────────────────
  inventory: overrides.inventory ?? []
});

// ============================================================================
// UTILIDADES
// ============================================================================

/**
 * Obtener rango del perfil
 */
export const getProfileRank = (profile) => {
  return getRank(profile?.rating || 1500);
};

/**
 * Calcular winrate
 */
export const getWinRate = (profile) => {
  if (!profile || profile.totalGames === 0) return 0;
  return Math.round((profile.wins / profile.totalGames) * 100);
};

/**
 * Verificar si está en placement
 */
export const isInPlacement = (profile) => {
  return profile?.isInPlacement && (profile?.placementGames || 0) < 10;
};

/**
 * Obtener progreso de placement
 */
export const getPlacementProgress = (profile) => {
  const games = profile?.placementGames || 0;
  return {
    gamesPlayed: games,
    gamesRemaining: Math.max(0, 10 - games),
    progress: Math.round((games / 10) * 100),
    wins: profile?.placementWins || 0
  };
};

/**
 * Crear perfil mínimo para oponentes
 */
export const createMinimalProfile = (name, rating = 1500) => ({
  id: `opponent_${Date.now()}_${Math.random().toString(36).substr(2, 6)}`,
  name,
  rating,
  avatar: '🎮',
  isAI: false
});

/**
 * Crear perfil de CPU
 */
export const createCPUProfile = (name, difficulty = 'medium') => {
  const ratings = { easy: 1200, medium: 1500, hard: 1800 };
  return {
    id: `cpu_${Date.now()}_${Math.random().toString(36).substr(2, 6)}`,
    name,
    rating: ratings[difficulty] || 1500,
    avatar: '🤖',
    isAI: true,
    difficulty
  };
};

export default {
  createDefaultPlayerProfile,
  getProfileRank,
  getWinRate,
  isInPlacement,
  getPlacementProgress,
  createMinimalProfile,
  createCPUProfile
};
